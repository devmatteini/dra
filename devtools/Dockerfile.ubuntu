FROM ubuntu:20.04
ARG GITHUB_TOKEN
ENV GITHUB_TOKEN=$GITHUB_TOKEN
RUN apt-get update && apt-get install -y unzip xz-utils && rm -rf /var/lib/apt/lists/*
# Add user with no root permissions
RUN useradd -ms /bin/bash tester
COPY ./target/debug/dra /usr/local/bin/dra
